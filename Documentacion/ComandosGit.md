# Commits sugeridos en espanol

## Estructura y base de datos

```bash
git add README.md .gitignore .env.example docker-compose.yml BaseDatos/
git commit -m "configurar estructura inicial y base de datos con docker"
```

## API y Postman

```bash
git add ApiBackend/ Postman/
git commit -m "agregar endpoints iniciales de api y coleccion postman"
```

## WebAdmin

```bash
git add WebAdmin/
git commit -m "agregar modulo web de usuarios estadisticas y reportes"
```

## AppMobil

```bash
git add AppMobil/
git commit -m "agregar aplicacion movil con modulos mesero cocina y caja"
```

## Reportes PDF/XLSX

```bash
git add ApiBackend/ WebAdmin/
git commit -m "agregar reportes descargables en pdf y xlsx"
```

## Documentacion final

```bash
git add README.md Documentacion/
git commit -m "agregar documentacion de ejecucion y checklist de entrega"
```
